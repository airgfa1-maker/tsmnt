# 后端 API 地址配置示例
# 
# 使用场景说明：
# 
# 1. 本地开发（localhost 访问）
#    BACKEND_API_URL=http://localhost:3001
#
# 2. 内网部署（通过内网 IP 访问）
#    BACKEND_API_URL=http://192.168.0.xxx:3001
#    （将 xxx 替换为实际的内网 IP）
#
# 3. 公网部署（通过域名访问）
#    BACKEND_API_URL=https://api.yourdomain.com
#
# 4. 生产环境（通过环境变量在启动时设置）
#    npm run dev -- --env BACKEND_API_URL=https://api.yourdomain.com

# 根据你的部署环境修改以下地址
# 默认值：本地开发环境
BACKEND_API_URL=http://localhost:3001
